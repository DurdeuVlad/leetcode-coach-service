# Domain documentation

This repository uses a single domain context.

Read root `CONTEXT.md` and relevant records under `docs/adr/` before changing
domain behavior. Missing files are created lazily when terminology or durable
architectural decisions are actually resolved.

Use canonical glossary terms in code, issues, and specifications. Surface ADR
conflicts explicitly instead of silently overriding them.
